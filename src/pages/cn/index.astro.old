---
const lang = 'cn'
import BaseLayout from '../../layouts/BaseLayout.astro'
import EcosystemOrbit3DWrapper from '../../components/EcosystemOrbit3DWrapper'
import Hero3DBackground from '../../components/Hero3DBackground'
import VideoHeroBackground from '../../components/VideoHeroBackground'
import DNA3DHelix from '../../components/DNA3DHelix'
import ParticleWave3D from '../../components/ParticleWave3D'
import AnimatedSphere3D from '../../components/AnimatedSphere3D'
import ScrollDrivenProduct from '../../components/ScrollDrivenProduct.astro'
import ExitIntentPopup from '../../components/ExitIntentPopup'
import QualityCertifications from '../../components/QualityCertifications.astro'
import EcosystemWithModal from '../../components/EcosystemWithModal'

const seoTitle = "Quang Minh越南 - 高质量畜牧和水产饲料"
const seoDescription = "Quang Minh专业生产和提供猪、家禽和水产的高质量饲料。成立于2008年，应用先进技术，出口到东南亚和欧洲。"
const seoKeywords = [
  "畜牧饲料",
  "猪饲料",
  "家禽饲料", 
  "水产饲料",
  "Quang Minh",
  "Quang Minh越南",
  "aluminum profiles",
  "aluminum sheets",
  "aluminum doors",
  "河南养殖",
  "高质量饲料",
  "A Group",
  "饲料出口"
]

const ecosystem = [
  {
    name: "A Group",
    logo: "/appejv-logo.png",
    description: "农业和食品领域的领先集团",
    isParent: true
  },
  {
    name: "Quang Minh",
    logo: "/appe-logo.png",
    description: "高质量畜牧饲料"
  },
  {
    name: "AFARM",
    logo: "/afarm-logo.png",
    description: "高科技畜牧农场"
  },
  {
    name: "AFOOD",
    logo: "/afood-logo.png",
    description: "安全食品加工"
  },
  {
    name: "AGRIL",
    logo: "/agril-logo.png",
    description: "五星级餐厅连锁"
  },
  {
    name: "APHAMA",
    logo: "/aphama-logo.png",
    description: "兽医药和生物学"
  },
  {
    name: "AVAC",
    logo: "/avac-logo.png",
    description: "疫苗和生物制品"
  }
]

const stats = [
  { number: "2008", label: "成立年份", countUp: false },
  { number: "1500", label: "信赖客户", countUp: true, suffix: "+" },
  { number: "50000", label: "吨产量/年", countUp: true, suffix: "+" },
  { number: "98", label: "% 客户满意度", countUp: true, suffix: "%" }
]

const testimonials = [
  {
    name: "阮文成先生",
    role: "农场主，河南",
    avatar: "👨‍🌾",
    rating: 5,
    quote: "使用Quang Minh饲料2年，猪群发展很好，FCR比率显著降低。技术团队热情支持。"
  },
  {
    name: "陈氏梅女士",
    role: "家禽农场，南定",
    avatar: "👩‍🌾",
    rating: 5,
    quote: "产品质量稳定，鸡产蛋更多，更健康。价格合理，准时交货。"
  },
  {
    name: "范德龙先生",
    role: "水产养殖，太平",
    avatar: "🧑‍🌾",
    rating: 5,
    quote: "高质量鱼饲料，鱼发展均匀，少疾病。对Quang Minh服务非常满意。"
  }
]

const faqs = [
  {
    question: "Quang Minh产品有什么认证？",
    answer: "我们所有产品均通过ISO 9001:2015、HACCP和国际质量标准认证。我们致力于食品安全和产品质量。"
  },
  {
    question: "如何购买Quang Minh产品？",
    answer: "Quang Minh通过全国150+经销商网络分销产品。您可以在网站上找到最近的经销商或联系热线035 135 9520获得支持。"
  },
  {
    question: "如何获得技术咨询？",
    answer: "您可以联系热线035 135 9520或通过网站发送请求。我们的技术团队随时准备24/7支持。"
  },
  {
    question: "Quang Minh是否有大客户激励计划？",
    answer: "是的，我们为大量购买的客户提供优惠价格政策和特殊技术支持。请联系以获得详细建议。"
  },
  {
    question: "产品保质期多长？",
    answer: "产品保质期为3-6个月，具体取决于类型，在干燥、凉爽的地方妥善保存。"
  }
]

const features = [
  {
    title: "高质量",
    description: "产品符合国际标准，确保安全和有效"
  },
  {
    title: "先进技术",
    description: "应用现代技术和生产标准"
  },
  {
    title: "可持续合作",
    description: "承诺与客户和合作伙伴可持续发展"
  },
  {
    title: "出口国际",
    description: "产品出口到东南亚和欧洲"
  }
]

const products = [
  {
    name: "Pig Feed",
    title: "猪饲料",
    subtitle: "高级猪饲料",
    description: "每个发展阶段的最佳营养配方，支持最大生长和抵抗力",
    features: [
      "来自高质量来源的18-20%蛋白质",
      "平衡的维生素和矿物质",
      "支持消化和快速增重",
      "绝对安全，无禁用物质"
    ],
    image: "/pig-feed.svg",
    color: "#e11d48"
  },
  {
    name: "Poultry Feed",
    title: "家禽饲料",
    subtitle: "专业家禽饲料",
    description: "鸡鸭的平衡营养，帮助牲畜保持健康，均匀发展，高生产力",
    features: [
      "每个阶段的最佳能量",
      "增强自然抵抗力",
      "改善蛋和肉的质量",
      "降低死亡率，提高经济效益"
    ],
    image: "/poultry-feed.svg",
    color: "#f59e0b"
  },
  {
    name: "Fish Feed",
    title: "水产饲料",
    subtitle: "出口水产饲料",
    description: "满足国内水产养殖需求和出口到东南亚、欧洲的国际标准",
    features: [
      "每种鱼类的特殊配方",
      "最佳饲料转化率(FCR)",
      "不污染水环境",
      "出口到东南亚和欧洲"
    ],
    image: "/fish-feed.svg",
    color: "#175ead"
  }
]

const values = [
  {
    title: "愿景",
    description: "成为越南和地区畜牧和水产饲料生产领域的信誉企业"
  },
  {
    title: "使命",
    description: "提供高质量、稳定的产品，应用先进技术，与客户共同可持续发展"
  }
]
---

<BaseLayout 
  title={seoTitle}
  description={seoDescription}
  keywords={seoKeywords}
  ogImage="/appejv-logo.png"
  canonical="/"
>
  <div class="min-h-screen bg-white">
  <!-- Hero Section - Apple Style with Video Background -->
  <section class="min-h-screen flex items-center justify-center px-4 pt-24 pb-20 relative overflow-hidden" id="hero-section">
    <!-- Video Background with Fallback -->
    <VideoHeroBackground 
      videoUrl="/factory-video.mp4"
      fallbackImage="/hero-fallback.jpg"
      overlayOpacity={0.85}
      client:only="react"
    />
    
    <!-- 3D Enhanced Background (as secondary layer) -->
    <div class="absolute inset-0 opacity-20">
      <Hero3DBackground client:only="react" />
    </div>
    
    <!-- Multi-layer gradient overlay -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-50/60 via-white/70 to-green-50/60"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-white/90 via-transparent to-transparent"></div>
    
    <!-- Radial gradient spotlight -->
    <div class="absolute inset-0 bg-radial-gradient"></div>
    
    <!-- Animated gradient mesh with parallax -->
    <div class="absolute inset-0 opacity-30 parallax-layer" data-speed="0.5">
      <div class="absolute top-0 left-1/4 w-96 h-96 bg-blue-400 rounded-full mix-blend-multiply filter blur-3xl animate-blob"></div>
      <div class="absolute top-0 right-1/4 w-96 h-96 bg-green-400 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-2000"></div>
      <div class="absolute bottom-0 left-1/3 w-96 h-96 bg-indigo-400 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-4000"></div>
    </div>
    
    <div class="container mx-auto text-center relative z-10">
      <!-- New Badge -->
      <div class="mb-6 opacity-0 hero-fade-in" style="animation-delay: 0.1s">
        <div class="inline-flex items-center gap-2 bg-gradient-to-r from-blue-600 to-green-600 text-white px-5 py-2.5 rounded-full text-sm font-bold shadow-lg">
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
            <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
          </svg>
          <span>信誉品牌16+年 • ISO 9001:2015</span>
        </div>
      </div>
      
      <!-- Main Heading - Value Proposition with Numbers -->
      <h1 class="text-5xl md:text-7xl lg:text-8xl font-bold text-gray-900 mb-6 leading-[1.1] opacity-0 hero-fade-in tracking-tight parallax-text" style="animation-delay: 0.3s" data-speed="0.3">
        提高 <span class="bg-gradient-to-r from-green-600 to-blue-600 bg-clip-text text-transparent">30%</span> 效率<br />
        您的畜牧养殖
      </h1>
      
      <!-- Subheading - Clear Benefits -->
      <p class="text-xl md:text-2xl lg:text-3xl text-gray-700 mb-4 max-w-4xl mx-auto leading-relaxed opacity-0 hero-fade-in font-medium parallax-text" style="animation-delay: 0.5s" data-speed="0.2">
        使用Quang Minh的高科技饲料
      </p>
      
      <div class="flex flex-col md:flex-row md:flex-wrap md:justify-center gap-3 md:gap-0 text-base md:text-lg lg:text-xl text-gray-600 mb-10 max-w-3xl mx-auto opacity-0 hero-fade-in font-light" style="animation-delay: 0.6s">
        <span class="flex items-center gap-2 md:mx-2">
          <svg class="w-5 h-5 flex-shrink-0 text-green-600" fill="currentColor" viewBox="0 0 20 20">
            <path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clipRule="evenodd" />
          </svg>
          <span>更快增重</span>
        </span>
        <span class="flex items-center gap-2 md:mx-2">
          <svg class="w-5 h-5 flex-shrink-0 text-green-600" fill="currentColor" viewBox="0 0 20 20">
            <path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clipRule="evenodd" />
          </svg>
          <span>降低饲料成本</span>
        </span>
        <span class="flex items-center gap-2 md:mx-2">
          <svg class="w-5 h-5 flex-shrink-0 text-green-600" fill="currentColor" viewBox="0 0 20 20">
            <path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clipRule="evenodd" />
          </svg>
          <span>更健康的牲畜</span>
        </span>
      </div>
      
      <!-- Enhanced CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center opacity-0 hero-fade-in mb-16" style="animation-delay: 0.8s">
        <a 
          href="/cn/products"
          class="group bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-10 py-5 rounded-full font-bold text-lg transition-all inline-flex items-center justify-center gap-3 shadow-xl hover:shadow-2xl hover:scale-105 transform"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
          </svg>
          <span>查看产品和价格</span>
          <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
        <a 
          href="/cn/contact"
          class="group bg-white hover:bg-gray-50 text-gray-900 px-10 py-5 rounded-full font-bold text-lg border-2 border-gray-300 hover:border-gray-400 transition-all inline-flex items-center justify-center gap-3 shadow-lg hover:shadow-xl hover:scale-105 transform"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
          </svg>
          <span>24/7免费咨询</span>
        </a>
      </div>
      
      <!-- Enhanced Trust Indicators with Stats -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-6 max-w-5xl mx-auto opacity-0 hero-fade-in" style="animation-delay: 1s">
        <div class="bg-white/95 backdrop-blur-md rounded-xl md:rounded-2xl p-4 md:p-6 shadow-xl border border-gray-200 hover:shadow-2xl transition-all hover:-translate-y-1 hover:border-green-300">
          <div class="flex items-center justify-center mb-2 md:mb-3">
            <div class="w-12 h-12 md:w-14 md:h-14 bg-gradient-to-br from-green-100 to-green-200 rounded-full flex items-center justify-center">
              <svg class="w-6 h-6 md:w-8 md:h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
          </div>
          <div class="text-xl md:text-2xl font-bold text-gray-900 mb-1">ISO 9001</div>
          <div class="text-xs md:text-sm text-gray-600 font-medium">国际认证</div>
        </div>
        
        <div class="bg-white/95 backdrop-blur-md rounded-xl md:rounded-2xl p-4 md:p-6 shadow-xl border border-gray-200 hover:shadow-2xl transition-all hover:-translate-y-1 hover:border-blue-300">
          <div class="flex items-center justify-center mb-2 md:mb-3">
            <div class="w-12 h-12 md:w-14 md:h-14 bg-gradient-to-br from-blue-100 to-blue-200 rounded-full flex items-center justify-center">
              <svg class="w-6 h-6 md:w-8 md:h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
          </div>
          <div class="text-xl md:text-2xl font-bold text-gray-900 mb-1">16+年</div>
          <div class="text-xs md:text-sm text-gray-600 font-medium">年经验</div>
        </div>
        
        <div class="bg-white/95 backdrop-blur-md rounded-xl md:rounded-2xl p-4 md:p-6 shadow-xl border border-gray-200 hover:shadow-2xl transition-all hover:-translate-y-1 hover:border-purple-300">
          <div class="flex items-center justify-center mb-2 md:mb-3">
            <div class="w-12 h-12 md:w-14 md:h-14 bg-gradient-to-br from-purple-100 to-purple-200 rounded-full flex items-center justify-center">
              <svg class="w-6 h-6 md:w-8 md:h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
              </svg>
            </div>
          </div>
          <div class="text-xl md:text-2xl font-bold text-gray-900 mb-1">1,500+</div>
          <div class="text-xs md:text-sm text-gray-600 font-medium">客户</div>
        </div>
        
        <div class="bg-white/95 backdrop-blur-md rounded-xl md:rounded-2xl p-4 md:p-6 shadow-xl border border-gray-200 hover:shadow-2xl transition-all hover:-translate-y-1 hover:border-orange-300">
          <div class="flex items-center justify-center mb-2 md:mb-3">
            <div class="w-12 h-12 md:w-14 md:h-14 bg-gradient-to-br from-orange-100 to-orange-200 rounded-full flex items-center justify-center">
              <svg class="w-6 h-6 md:w-8 md:h-8 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
          </div>
          <div class="text-xl md:text-2xl font-bold text-gray-900 mb-1">出口</div>
          <div class="text-xs md:text-sm text-gray-600 font-medium">东南亚和欧洲</div>
        </div>
      </div>
      
      <!-- Scroll indicator with animation -->
      <div class="mt-16 opacity-0 hero-fade-in scroll-indicator" style="animation-delay: 1.2s">
        <div class="flex flex-col items-center gap-2">
          <div class="w-6 h-10 border-2 border-gray-400 rounded-full flex items-start justify-center p-2 hover:border-gray-600 transition-colors cursor-pointer">
            <div class="w-1.5 h-3 bg-gray-500 rounded-full animate-bounce"></div>
          </div>
          <span class="text-xs text-gray-500 font-semibold tracking-wide uppercase">了解更多</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Section - Apple Style Enhanced -->
  <section class="py-32 relative overflow-hidden">
    <!-- Multi-layer background -->
    <div class="absolute inset-0 bg-gradient-to-br from-gray-50 via-blue-50/30 to-gray-50"></div>
    
    <!-- Animated gradient orbs -->
    <div class="absolute inset-0 opacity-20">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-blue-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob"></div>
      <div class="absolute top-1/3 right-1/4 w-96 h-96 bg-indigo-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-2000"></div>
    </div>
    
    <div class="container mx-auto px-4 relative z-10">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-12 md:gap-16">
        {stats.map(stat => (
          <div class="text-center stat-item">
            <div 
              class="text-5xl md:text-6xl font-semibold text-gray-900 mb-3 stat-number tracking-tight" 
              data-target={stat.number}
              data-countup={stat.countUp}
              data-suffix={stat.suffix || ''}
            >
              {stat.countUp ? '0' : stat.number}
            </div>
            <div class="text-base md:text-lg text-gray-500 font-light">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Vision & Mission - Apple Style with Enhanced Background -->
  <section class="py-32 px-4 relative overflow-hidden">
    <!-- Base gradient -->
    <div class="absolute inset-0 bg-white"></div>
    
    <!-- Radial gradient overlay -->
    <div class="absolute inset-0 bg-radial-gradient-purple"></div>
    
    <!-- Animated mesh gradient -->
    <div class="absolute inset-0 opacity-25">
      <div class="absolute bottom-0 left-0 w-96 h-96 bg-purple-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob"></div>
      <div class="absolute top-1/4 right-1/4 w-96 h-96 bg-blue-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-4000"></div>
    </div>
    
    <!-- 3D DNA Helix Background -->
    <div class="absolute right-0 top-1/2 transform -translate-y-1/2 opacity-15 hidden lg:block">
      <DNA3DHelix client:only="react" />
    </div>
    
    <div class="container mx-auto max-w-6xl relative z-10">
      <div class="grid md:grid-cols-2 gap-16 md:gap-24">
        {values.map((value, index) => (
          <div class="text-center md:text-left scroll-reveal">
            <h3 class="text-5xl md:text-6xl font-semibold text-gray-900 mb-6 tracking-tight">{value.title}</h3>
            <p class="text-xl md:text-2xl text-gray-600 leading-relaxed font-light">{value.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Company Video - Apple Style Enhanced -->
  <section class="py-32 px-4 relative overflow-hidden">
    <!-- Gradient background -->
    <div class="absolute inset-0 bg-gradient-to-b from-white via-gray-50 to-white"></div>
    
    <!-- Subtle pattern overlay -->
    <div class="absolute inset-0 opacity-5" style="background-image: radial-gradient(circle at 1px 1px, rgb(0 0 0 / 0.15) 1px, transparent 0); background-size: 40px 40px;"></div>
    
    <div class="container mx-auto relative z-10">
      <div class="text-center mb-16 scroll-reveal">
        <h2 class="text-5xl md:text-6xl font-semibold text-gray-900 mb-6 tracking-tight">探索Quang Minh</h2>
        <p class="text-xl md:text-2xl text-gray-600 max-w-3xl mx-auto font-light leading-relaxed">
          我们提供高质量畜牧饲料的<br class="hidden md:block" />
          旅程和承诺
        </p>
      </div>
      <div class="max-w-5xl mx-auto scroll-reveal">
        <div class="relative aspect-video rounded-3xl overflow-hidden shadow-2xl bg-gray-900 ring-1 ring-gray-900/5">
          <iframe
            class="w-full h-full"
            src="https://www.youtube.com/embed/Wu_9WTZPMwY?si=GbagrIg01IBKqdse"
            title="YouTube video player"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
            referrerpolicy="strict-origin-when-cross-origin"
            allowfullscreen
          ></iframe>
        </div>
      </div>
    </div>
  </section>

  <!-- A Group Ecosystem - Apple Style Enhanced -->
  <section class="py-32 px-4 relative overflow-hidden">
    <!-- Base background -->
    <div class="absolute inset-0 bg-white"></div>
    
    <!-- Gradient overlay -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-50/40 via-white to-green-50/40"></div>
    
    <!-- Animated gradient mesh -->
    <div class="absolute inset-0 opacity-20">
      <div class="absolute top-0 left-1/3 w-96 h-96 bg-blue-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-2000"></div>
      <div class="absolute bottom-0 right-1/3 w-96 h-96 bg-green-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-4000"></div>
    </div>
    
    <div class="container mx-auto relative z-10">
      <div class="text-center mb-20 scroll-reveal">
        <h2 class="text-5xl md:text-6xl font-semibold text-gray-900 mb-6 tracking-tight">A Group生态系统</h2>
        <p class="text-xl md:text-2xl text-gray-600 max-w-4xl mx-auto font-light leading-relaxed">
          Quang Minh是A Group生态系统的成员 - 越南农业、<br class="hidden md:block" />
          畜牧和食品领域的领先集团
        </p>
      </div>

      <!-- 3D Visualization - Three.js Orbital Animation -->
      <div class="scroll-reveal">
        <EcosystemOrbit3DWrapper brands={ecosystem} client:only="react" />
      </div>

      <!-- Brand Cards with Modal -->
      <div class="mt-16 scroll-reveal">
        <EcosystemWithModal brands={ecosystem} client:load />
      </div>

      <div class="mt-16 text-center scroll-reveal">
        <p class="text-lg md:text-xl text-gray-600 mb-6 font-light">
          共同建设从农场到餐桌的可持续农业价值链
        </p>
        <div class="flex items-center justify-center gap-3">
          <span class="w-16 h-px bg-gradient-to-r from-transparent to-gray-300"></span>
          <span class="text-sm font-medium text-gray-500 tracking-wider">FARM TO FORK</span>
          <span class="w-16 h-px bg-gradient-to-l from-transparent to-gray-300"></span>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-20 px-4">
    <div class="container mx-auto">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold text-gray-900 mb-4">为什么选择Quang Minh？</h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">
          我们为畜牧业提供现代技术的综合解决方案
        </p>
      </div>
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {features.map((feature, index) => (
          <div class="border-0 shadow-lg hover:shadow-xl transition-shadow bg-white/80 backdrop-blur-sm rounded-2xl">
            <div class="text-center p-6">
              <div class="w-16 h-16 bg-gradient-to-r from-blue-500 to-green-500 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  {index === 0 && <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z" />}
                  {index === 1 && <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />}
                  {index === 2 && <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />}
                  {index === 3 && <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />}
                </svg>
              </div>
              <h3 class="text-xl font-bold text-gray-900 mb-3">{feature.title}</h3>
              <p class="text-gray-600">{feature.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Products Section - Apple-Style with Category Cards -->
  <section class="py-32 px-4 relative overflow-hidden">
    <!-- Base gradient -->
    <div class="absolute inset-0 bg-gradient-to-br from-gray-50 via-white to-gray-50"></div>
    
    <!-- 3D Wave Background -->
    <div class="absolute inset-0 opacity-8">
      <ParticleWave3D client:only="react" />
    </div>
    
    <div class="container mx-auto relative z-10">
      <div class="text-center mb-16 scroll-reveal">
        <h2 class="text-5xl md:text-6xl font-semibold text-gray-900 mb-6 tracking-tight">产品类别</h2>
        <p class="text-xl md:text-2xl text-gray-600 max-w-3xl mx-auto font-light leading-relaxed">
          高质量的畜牧和水产饲料产品，<br class="hidden md:block" />
          确保每个发展阶段的最佳营养
        </p>
      </div>

      <!-- Product Category Cards -->
      <div class="grid md:grid-cols-3 gap-6 md:gap-8 mb-16 scroll-reveal">
        {products.map((product, index) => (
          <a 
            href="/cn/products"
            class="group bg-white rounded-2xl md:rounded-3xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-500 hover:-translate-y-2"
          >
            <!-- Product Image -->
            <div class="relative h-48 md:h-64 overflow-hidden" style={`background: linear-gradient(135deg, ${product.color}15 0%, ${product.color}05 100%)`}>
              <div class="absolute inset-0 flex items-center justify-center p-6 md:p-8">
                <img 
                  src={product.image} 
                  alt={product.title}
                  class="w-full h-full object-contain transform group-hover:scale-110 transition-transform duration-700"
                />
              </div>
              <!-- Category Badge -->
              <div class="absolute top-3 left-3 md:top-4 md:left-4">
                <span class="bg-white/90 backdrop-blur-sm px-3 py-1.5 md:px-4 md:py-2 rounded-full text-xs md:text-sm font-semibold" style={`color: ${product.color}`}>
                  {product.name}
                </span>
              </div>
            </div>

            <!-- Product Info -->
            <div class="p-6 md:p-8">
              <h3 class="text-xl md:text-2xl font-bold text-gray-900 mb-2 md:mb-3 group-hover:text-blue-600 transition-colors">
                {product.title}
              </h3>
              <p class="text-sm md:text-base text-gray-600 mb-4 md:mb-6 leading-relaxed line-clamp-2 md:line-clamp-none">
                {product.description}
              </p>

              <!-- Key Features -->
              <div class="space-y-1.5 md:space-y-2 mb-4 md:mb-6">
                {product.features.slice(0, 3).map((feature) => (
                  <div class="flex items-start gap-2 text-xs md:text-sm text-gray-700">
                    <svg class="w-4 h-4 md:w-5 md:h-5 flex-shrink-0 mt-0.5" style={`color: ${product.color}`} fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                    </svg>
                    <span class="line-clamp-1">{feature}</span>
                  </div>
                ))}
              </div>

              <!-- View Details Link -->
              <div class="flex items-center gap-2 text-blue-600 font-semibold text-sm md:text-base group-hover:gap-3 transition-all">
                <span>查看详情</span>
                <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
              </div>
            </div>
          </a>
        ))}
      </div>

      <!-- Product Stats -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-12 scroll-reveal">
        <div class="text-center p-6 bg-white/80 backdrop-blur-sm rounded-2xl shadow-md">
          <div class="text-4xl font-bold text-blue-600 mb-2">50+</div>
          <div class="text-sm text-gray-600 font-medium">产品</div>
        </div>
        <div class="text-center p-6 bg-white/80 backdrop-blur-sm rounded-2xl shadow-md">
          <div class="text-4xl font-bold text-green-600 mb-2">3</div>
          <div class="text-sm text-gray-600 font-medium">主要类别</div>
        </div>
        <div class="text-center p-6 bg-white/80 backdrop-blur-sm rounded-2xl shadow-md">
          <div class="text-4xl font-bold text-purple-600 mb-2">100%</div>
          <div class="text-sm text-gray-600 font-medium">质量</div>
        </div>
        <div class="text-center p-6 bg-white/80 backdrop-blur-sm rounded-2xl shadow-md">
          <div class="text-4xl font-bold text-orange-600 mb-2">ISO</div>
          <div class="text-sm text-gray-600 font-medium">认证</div>
        </div>
      </div>

      <!-- CTA to Full Product Page -->
      <div class="text-center scroll-reveal">
        <a 
          href="/cn/products"
          class="inline-flex items-center gap-3 bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-10 py-5 rounded-full font-bold text-lg shadow-xl hover:shadow-2xl transition-all hover:scale-105 transform"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
          </svg>
          <span>查看所有产品和价格</span>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
        <p class="mt-4 text-sm text-gray-500">
          公开价格 • 经销商配送 • 24/7技术支持
        </p>
      </div>
    </div>
  </section>

  <!-- Quality Certifications Section -->
  <QualityCertifications lang="cn" />

  <!-- Testimonials Section - Apple Style Enhanced -->
  <section class="py-32 px-4 relative overflow-hidden">
    <!-- Base background -->
    <div class="absolute inset-0 bg-white"></div>
    
    <!-- Gradient overlay -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-50/30 via-white to-purple-50/30"></div>
    
    <!-- 3D Animated Sphere Background -->
    <div class="absolute left-0 top-1/2 transform -translate-y-1/2 opacity-8 hidden lg:block">
      <AnimatedSphere3D client:only="react" />
    </div>
    
    <!-- Animated gradient mesh -->
    <div class="absolute inset-0 opacity-15">
      <div class="absolute top-1/3 right-1/4 w-96 h-96 bg-purple-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-2000"></div>
      <div class="absolute bottom-1/3 left-1/3 w-96 h-96 bg-blue-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-4000"></div>
    </div>
    
    <div class="container mx-auto relative z-10">
      <div class="text-center mb-20 scroll-reveal">
        <h2 class="text-5xl md:text-6xl font-semibold text-gray-900 mb-6 tracking-tight">客户评价</h2>
        <p class="text-xl md:text-2xl text-gray-600 max-w-3xl mx-auto font-light leading-relaxed">
          数千客户信赖并与Quang Minh同行
        </p>
      </div>
      <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {testimonials.map((testimonial, index) => (
          <div class="bg-white/80 backdrop-blur-sm rounded-3xl p-10 hover:bg-white hover:shadow-2xl transition-all duration-500 scroll-reveal ring-1 ring-gray-900/5" style={`animation-delay: ${index * 0.1}s`}>
            <div class="flex mb-6">
              {Array.from({ length: testimonial.rating }).map(() => (
                <svg class="w-5 h-5 text-gray-900" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                </svg>
              ))}
            </div>
            <p class="text-lg text-gray-900 mb-8 leading-relaxed font-light">"{testimonial.quote}"</p>
            <div class="flex items-center">
              <div class="text-3xl mr-4">{testimonial.avatar}</div>
              <div>
                <h4 class="font-semibold text-gray-900">{testimonial.name}</h4>
                <p class="text-sm text-gray-500 font-light">{testimonial.role}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section - View Products & Find Dealers + Dealer Registration -->
  <section class="py-32 px-4 relative overflow-hidden">
    <!-- Gradient background -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-600 to-blue-700"></div>
    
    <!-- Animated background -->
    <div class="absolute inset-0 opacity-10">
      <div class="absolute top-0 left-0 w-96 h-96 bg-white rounded-full filter blur-3xl animate-blob"></div>
      <div class="absolute bottom-0 right-0 w-96 h-96 bg-white rounded-full filter blur-3xl animate-blob animation-delay-2000"></div>
      <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-white rounded-full filter blur-3xl animate-blob animation-delay-4000"></div>
    </div>
    
    <div class="container mx-auto relative z-10">
      <div class="max-w-6xl mx-auto">
        <!-- Tabs Navigation -->
        <div class="flex justify-center mb-12 scroll-reveal">
          <div class="inline-flex bg-white/10 backdrop-blur-sm rounded-full p-1.5 border border-white/20">
            <button 
              class="tab-button active px-8 py-3 rounded-full font-semibold text-white transition-all"
              data-tab="customer"
            >
              <span class="flex items-center gap-2">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                </svg>
                Dành cho khách hàng
              </span>
            </button>
            <button 
              class="tab-button px-8 py-3 rounded-full font-semibold text-white/70 hover:text-white transition-all"
              data-tab="dealer"
            >
              <span class="flex items-center gap-2">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
                </svg>
                注册成为经销商
              </span>
            </button>
          </div>
        </div>

        <!-- Customer Tab Content -->
        <div class="tab-content active" id="customer-tab">
          <div class="text-center text-white scroll-reveal">
            <h2 class="text-4xl md:text-5xl font-semibold mb-6 tracking-tight">
              准备好提高<br />养殖效率了吗？
            </h2>
            <p class="text-xl text-blue-100 mb-8 font-light leading-relaxed max-w-2xl mx-auto">
              查看产品价格并联系您附近的经销商进行咨询和购买
            </p>
            
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
              <a 
                href="/cn/products"
                class="bg-white hover:bg-gray-50 text-blue-600 px-10 py-4 rounded-full font-semibold text-lg transition-all shadow-lg hover:shadow-xl inline-flex items-center justify-center gap-2"
              >
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                </svg>
                查看产品和价格
              </a>
              <a 
                href="/cn/contact"
                class="bg-transparent border-2 border-white hover:bg-white/10 text-white px-10 py-4 rounded-full font-semibold text-lg transition-all inline-flex items-center justify-center gap-2"
              >
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                </svg>
                查找附近经销商
              </a>
            </div>

            <div class="mt-12 grid md:grid-cols-3 gap-6 text-left">
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/10">
                <div class="text-3xl mb-3">💰</div>
                <h3 class="font-semibold text-lg mb-2">公开价格</h3>
                <p class="text-blue-100 text-sm">在网站上查看所有产品价格，透明且有竞争力</p>
              </div>
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/10">
                <div class="text-3xl mb-3">🏪</div>
                <h3 class="font-semibold text-lg mb-2">经销商网络</h3>
                <p class="text-blue-100 text-sm">全国150+经销商，轻松查找和购买</p>
              </div>
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/10">
                <div class="text-3xl mb-3">🛠️</div>
                <h3 class="font-semibold text-lg mb-2">技术支持</h3>
                <p class="text-blue-100 text-sm">专家团队提供24/7免费咨询</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Dealer Tab Content -->
        <div class="tab-content" id="dealer-tab">
          <div class="text-center text-white scroll-reveal">
            <h2 class="text-4xl md:text-5xl font-semibold mb-6 tracking-tight">
              成为Quang Minh<br />分销合作伙伴
            </h2>
            <p class="text-xl text-blue-100 mb-8 font-light leading-relaxed max-w-2xl mx-auto">
              高质量产品和全面支持的诱人商机
            </p>
            
            <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
              <a 
                href="/lien-he?type=dealer"
                class="bg-white hover:bg-gray-50 text-blue-600 px-10 py-4 rounded-full font-bold text-lg transition-all shadow-lg hover:shadow-xl inline-flex items-center justify-center gap-2"
              >
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                </svg>
                立即注册
              </a>
              <a 
                href="tel:+84947776662"
                class="bg-transparent border-2 border-white hover:bg-white/10 text-white px-10 py-4 rounded-full font-bold text-lg transition-all inline-flex items-center justify-center gap-2"
              >
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                </svg>
                致电：035 135 9520
              </a>
            </div>

            <div class="grid md:grid-cols-4 gap-6 text-left">
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/10">
                <div class="text-3xl mb-3">🎯</div>
                <h3 class="font-semibold text-lg mb-2">诱人政策</h3>
                <p class="text-blue-100 text-sm">高佣金，营销和销售支持</p>
              </div>
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/10">
                <div class="text-3xl mb-3">📦</div>
                <h3 class="font-semibold text-lg mb-2">多样化产品</h3>
                <p class="text-blue-100 text-sm">猪、家禽、水产养殖的完整产品线</p>
              </div>
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/10">
                <div class="text-3xl mb-3">🎓</div>
                <h3 class="font-semibold text-lg mb-2">免费培训</h3>
                <p class="text-blue-100 text-sm">产品知识和销售技能</p>
              </div>
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/10">
                <div class="text-3xl mb-3">🤝</div>
                <h3 class="font-semibold text-lg mb-2">全面支持</h3>
                <p class="text-blue-100 text-sm">销售团队24/7支持，快速交付</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <script>
    // Tab switching functionality
    document.addEventListener('DOMContentLoaded', () => {
      const tabButtons = document.querySelectorAll('.tab-button');
      const tabContents = document.querySelectorAll('.tab-content');
      
      tabButtons.forEach(button => {
        button.addEventListener('click', () => {
          const tabName = button.getAttribute('data-tab');
          
          // Remove active class from all buttons and contents
          tabButtons.forEach(btn => btn.classList.remove('active', 'bg-white/20'));
          tabContents.forEach(content => content.classList.remove('active'));
          
          // Add active class to clicked button and corresponding content
          button.classList.add('active', 'bg-white/20');
          document.getElementById(`${tabName}-tab`).classList.add('active');
        });
      });
    });
  </script>

  <!-- FAQ Section - Apple Style Enhanced -->
  <section class="py-32 px-4 relative overflow-hidden">
    <!-- Gradient background -->
    <div class="absolute inset-0 bg-gradient-to-b from-white via-gray-50 to-white"></div>
    
    <!-- Animated gradient orbs -->
    <div class="absolute inset-0 opacity-15">
      <div class="absolute top-0 left-1/4 w-96 h-96 bg-indigo-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob"></div>
      <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-blue-300 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-2000"></div>
    </div>
    
    <div class="container mx-auto max-w-4xl relative z-10">
      <div class="text-center mb-20 scroll-reveal">
        <h2 class="text-5xl md:text-6xl font-semibold text-gray-900 mb-6 tracking-tight">常见问题</h2>
        <p class="text-xl md:text-2xl text-gray-600 font-light leading-relaxed">
          关于产品和服务的常见问题解答
        </p>
      </div>
      <div class="space-y-2">
        {faqs.map((faq, index) => (
          <details class="group bg-white/80 backdrop-blur-sm rounded-2xl overflow-hidden hover:shadow-lg transition-all duration-300 scroll-reveal ring-1 ring-gray-900/5" style={`animation-delay: ${index * 0.05}s`}>
            <summary class="flex justify-between items-center cursor-pointer p-8 font-semibold text-gray-900 text-lg md:text-xl">
              <span class="pr-8">{faq.question}</span>
              <svg class="w-6 h-6 text-gray-400 transition-transform group-open:rotate-180 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </summary>
            <div class="px-8 pb-8 text-gray-600 text-lg leading-relaxed font-light">
              {faq.answer}
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- Mobile Sticky CTA - Hidden -->
  <!-- Removed to avoid clutter on mobile -->

  <!-- Sticky CTA Bar - Desktop -->
  <div id="sticky-cta" class="fixed bottom-0 left-0 right-0 bg-white/95 backdrop-blur-xl border-t border-gray-200 py-4 px-4 z-40 transform translate-y-full transition-transform duration-300 hidden md:block">
    <div class="container mx-auto flex items-center justify-between">
      <div>
        <div class="font-semibold text-gray-900">准备好提高养殖效率了吗？</div>
        <div class="text-sm text-gray-600">立即联系以获得专家的免费咨询</div>
      </div>
      <div class="flex gap-3">
        <a href="tel:+84947776662" class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-full font-medium transition-colors inline-flex items-center gap-2 shadow-sm">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
          </svg>
          立即致电
        </a>
        <a href="/cn/contact" class="px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-full font-medium transition-colors shadow-sm">
          注册咨询
        </a>
      </div>
    </div>
  </div>

  </div>

  <!-- Exit Intent Popup -->
  <ExitIntentPopup client:load />

  <!-- Count-up Animation Script -->
  <script>
    function animateCountUp() {
      const statNumbers = document.querySelectorAll('.stat-number');
      
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting && !entry.target.classList.contains('counted')) {
            const target = entry.target;
            const shouldCountUp = target.dataset.countup === 'true';
            
            if (shouldCountUp) {
              const finalValue = parseInt(target.dataset.target);
              const suffix = target.dataset.suffix || '';
              const duration = 2000;
              const steps = 60;
              const increment = finalValue / steps;
              let current = 0;
              
              const timer = setInterval(() => {
                current += increment;
                if (current >= finalValue) {
                  target.textContent = finalValue.toLocaleString() + suffix;
                  clearInterval(timer);
                } else {
                  target.textContent = Math.floor(current).toLocaleString() + suffix;
                }
              }, duration / steps);
              
              target.classList.add('counted');
            }
          }
        });
      }, { threshold: 0.5 });
      
      statNumbers.forEach(stat => observer.observe(stat));
    }
    
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', animateCountUp);
    } else {
      animateCountUp();
    }
  </script>

  <!-- Scroll Reveal Animations -->
  <script>
    function initScrollReveal() {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('revealed');
          }
        });
      }, { 
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
      });
      
      document.querySelectorAll('.scroll-reveal').forEach(el => {
        observer.observe(el);
      });
    }
    
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initScrollReveal);
    } else {
      initScrollReveal();
    }
  </script>

  <!-- Enhanced Parallax Scroll Effects -->
  <script>
    let ticking = false;
    
    function updateParallax() {
      const scrolled = window.pageYOffset;
      
      // Parallax layers in hero
      const parallaxLayers = document.querySelectorAll('.parallax-layer');
      parallaxLayers.forEach(layer => {
        const speed = parseFloat(layer.dataset.speed) || 0.5;
        const yPos = -(scrolled * speed);
        layer.style.transform = `translateY(${yPos}px)`;
      });
      
      // Parallax text elements
      const parallaxTexts = document.querySelectorAll('.parallax-text');
      parallaxTexts.forEach(text => {
        const speed = parseFloat(text.dataset.speed) || 0.3;
        const yPos = -(scrolled * speed);
        text.style.transform = `translateY(${yPos}px)`;
      });
      
      // Hide scroll indicator after scrolling
      const scrollIndicator = document.querySelector('.scroll-indicator');
      if (scrollIndicator) {
        if (scrolled > 100) {
          scrollIndicator.style.opacity = '0';
          scrollIndicator.style.pointerEvents = 'none';
        } else {
          scrollIndicator.style.opacity = '1';
          scrollIndicator.style.pointerEvents = 'auto';
        }
      }
      
      // Scale effect on sections
      const sections = document.querySelectorAll('section');
      sections.forEach(section => {
        const rect = section.getBoundingClientRect();
        const scrollProgress = 1 - (rect.top / window.innerHeight);
        
        if (scrollProgress > 0 && scrollProgress < 1) {
          const scale = 0.95 + (scrollProgress * 0.05);
          const opacity = Math.max(0.3, scrollProgress);
          
          // Apply subtle scale and opacity
          if (section.id !== 'hero-section') {
            section.style.transform = `scale(${Math.min(scale, 1)})`;
            section.style.opacity = opacity;
          }
        }
      });
      
      ticking = false;
    }
    
    function requestTick() {
      if (!ticking) {
        window.requestAnimationFrame(updateParallax);
        ticking = true;
      }
    }
    
    window.addEventListener('scroll', requestTick);
    
    // Initial call
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', updateParallax);
    } else {
      updateParallax();
    }
  </script>

  <!-- Sticky CTA Bar Script -->
  <script>
    let lastScroll = 0;
    const stickyCTA = document.getElementById('sticky-cta');
    const heroHeight = window.innerHeight;
    
    window.addEventListener('scroll', () => {
      const currentScroll = window.pageYOffset;
      
      // Show sticky CTA after scrolling past hero
      if (currentScroll > heroHeight && currentScroll > lastScroll) {
        stickyCTA?.classList.remove('translate-y-full');
      } else if (currentScroll < lastScroll || currentScroll < heroHeight) {
        stickyCTA?.classList.add('translate-y-full');
      }
      
      lastScroll = currentScroll;
    });
  </script>

  <style>
    /* Apple-style Typography */
    body {
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    /* Hero Fade-in Animation */
    @keyframes heroFadeIn {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .hero-fade-in {
      animation: heroFadeIn 1s ease-out forwards;
    }

    /* Fade In Animation */
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    
    .animate-fadeIn {
      animation: fadeIn 0.5s ease-out forwards;
    }

    /* Fade Out Animation */
    @keyframes fadeOut {
      from {
        opacity: 1;
      }
      to {
        opacity: 0;
      }
    }
    
    .animate-fadeOut {
      animation: fadeOut 0.3s ease-out forwards;
    }

    /* Scale In Animation */
    @keyframes scaleIn {
      from {
        opacity: 0;
        transform: scale(0.9);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }
    
    .animate-scaleIn {
      animation: scaleIn 0.3s ease-out forwards;
    }

    /* Scale Out Animation */
    @keyframes scaleOut {
      from {
        opacity: 1;
        transform: scale(1);
      }
      to {
        opacity: 0;
        transform: scale(0.95);
      }
    }
    
    .animate-scaleOut {
      animation: scaleOut 0.3s ease-out forwards;
    }

    /* Scroll Reveal Animation */
    .scroll-reveal {
      opacity: 0;
      transform: translateY(40px);
      transition: opacity 0.8s ease-out, transform 0.8s ease-out;
    }
    
    .scroll-reveal.revealed {
      opacity: 1;
      transform: translateY(0);
    }

    /* Blob Animation for gradient mesh */
    @keyframes blob {
      0%, 100% {
        transform: translate(0, 0) scale(1);
      }
      25% {
        transform: translate(20px, -50px) scale(1.1);
      }
      50% {
        transform: translate(-20px, 20px) scale(0.9);
      }
      75% {
        transform: translate(50px, 50px) scale(1.05);
      }
    }
    
    .animate-blob {
      animation: blob 20s infinite;
    }
    
    .animation-delay-2000 {
      animation-delay: 2s;
    }
    
    .animation-delay-4000 {
      animation-delay: 4s;
    }

    /* Radial gradient */
    .bg-radial-gradient {
      background: radial-gradient(
        circle at 50% 50%,
        rgba(59, 130, 246, 0.1) 0%,
        transparent 50%
      );
    }
    
    /* Radial gradient purple */
    .bg-radial-gradient-purple {
      background: radial-gradient(
        circle at 30% 50%,
        rgba(147, 51, 234, 0.08) 0%,
        transparent 50%
      );
    }

    /* Smooth Scroll */
    html {
      scroll-behavior: smooth;
    }

    /* Parallax layers */
    .parallax-layer {
      will-change: transform;
      transition: transform 0.1s ease-out;
    }
    
    .parallax-text {
      will-change: transform;
      transition: transform 0.1s ease-out;
    }

    /* Scroll indicator fade */
    .scroll-indicator {
      transition: opacity 0.5s ease-out;
    }

    /* Section transitions */
    section {
      transition: transform 0.3s ease-out, opacity 0.3s ease-out;
      will-change: transform, opacity;
    }

    /* Hide mobile CTA on desktop */
    @media (min-width: 768px) {
      .mobile-cta {
        display: none;
      }
    }
    
    /* Bounce animation for phone button */
    @keyframes bounce-slow {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }
    
    .animate-bounce-slow {
      animation: bounce-slow 2s infinite;
    }

    /* Apple-style button hover effects */
    a[href] {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    /* Backdrop blur support */
    @supports (backdrop-filter: blur(20px)) {
      .mobile-cta {
        background-color: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(20px);
      }
    }

    /* Remove default details marker */
    details > summary {
      list-style: none;
    }
    
    details > summary::-webkit-details-marker {
      display: none;
    }

    /* Smooth transitions for details */
    details {
      transition: all 0.3s ease;
    }

    /* Custom scrollbar (Apple-like) */
    ::-webkit-scrollbar {
      width: 12px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: rgba(0, 0, 0, 0.2);
      border-radius: 6px;
      border: 3px solid transparent;
      background-clip: padding-box;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: rgba(0, 0, 0, 0.3);
      background-clip: padding-box;
    }

    /* Prevent layout shift */
    section {
      contain: layout;
    }

    /* Smooth scale animations */
    @media (prefers-reduced-motion: no-preference) {
      section {
        transform-origin: center center;
      }
    }

    /* Disable parallax on mobile for performance */
    @media (max-width: 768px) {
      .parallax-layer,
      .parallax-text {
        transform: none !important;
      }
      
      /* Better mobile spacing */
      section {
        padding-top: 3rem;
        padding-bottom: 3rem;
      }
      
      /* Optimize touch targets */
      button, a {
        min-height: 44px;
      }
      
      /* Line clamp utilities */
      .line-clamp-1 {
        display: -webkit-box;
        -webkit-line-clamp: 1;
        -webkit-box-orient: vertical;
        overflow: hidden;
      }
      
      .line-clamp-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
      }
    }

    /* Tab Styles */
    .tab-content {
      display: none;
      animation: fadeIn 0.5s ease-out;
    }

    .tab-content.active {
      display: block;
    }

    .tab-button.active {
      background-color: rgba(255, 255, 255, 0.2);
    }
  </style>
</BaseLayout>
